## suite_view_generator
Generates test suite result view.
### Install
Run command `npm install` from plugin root directory.
### Usage
Run command `npm start` from plugin root directory to generate html file.
